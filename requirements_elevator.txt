# ══════════════════════════════════════════════════════════════
# VozElevate — requirements.txt
# ══════════════════════════════════════════════════════════════
# Instalar con:  pip install -r requirements.txt

# ── Interfaz ──────────────────────────────────────────────────
streamlit==1.35.0

# ── Reconocimiento de voz (Web Speech API bridge) ─────────────
bokeh==2.4.3
streamlit-bokeh-events==0.1.2

# ── Canvas para dibujo ────────────────────────────────────────
streamlit-drawable-canvas==0.9.3

# ── Red neuronal (reconocimiento de dígitos) ──────────────────
tensorflow==2.15.0          # usa tensorflow-cpu si no tienes GPU
# tensorflow-cpu==2.15.0   # alternativa más liviana

# ── Procesamiento de imágenes ─────────────────────────────────
Pillow==10.3.0
numpy==1.26.4

# ── MQTT (comunicación con Wokwi/Arduino) ─────────────────────
paho-mqtt==1.6.1

# ── Visualización (heredado de hand_writing.py, opcional) ─────
matplotlib==3.9.0
pandas==2.2.2
